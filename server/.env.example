PORT=3000
SALT=
PEPPER=
DATABASE_URL=postgres://user:pass@localhost:5432/nocc

# Only used by docker-compose.yml, for the containerized Postgres's password.
# Not read by the app itself (DATABASE_URL above is what the app uses locally;
# Docker Compose overrides DATABASE_URL to point at its own "db" container).
DB_PASSWORD=
